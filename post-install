if [ -d ${FW_REPOLOCAL}/firmware ]; then
        echo "     /lib/firmware"
        cp -R "${FW_REPOLOCAL}/firmware/"* /lib/firmware/
fi

sed 's/ttyAMA0/null/' /boot/cmdline.txt > /boot/cmdline.txt
sed '/getty -L ttyAMA0/d' /etc/inittab > /etc/inittab